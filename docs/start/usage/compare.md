# fox compare
Compare two files.

## Usage
```console
fox compare [FLAG ...] FILE1 FILE2
```

### Aliases
`diff`, `co`

### Arguments
Files to open

### Additional flags
- `-g`, `--git` — use the unified git diff format

## Example
```console
$ fox compare server.log mirror.log
```
